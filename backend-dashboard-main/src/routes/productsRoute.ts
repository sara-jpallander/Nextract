import express from "express"

const router = express.Router()



router.get("/", ) // Get all products
router.get("/:productId", ) //  get specific product by ID
router.put("/:productId", ) // Update products list
router.delete("/:productId", ) // Delete product form list



export default router;
