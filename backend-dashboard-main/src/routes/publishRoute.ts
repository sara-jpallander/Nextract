import express  from "express";

const router = express.Router()

router.get("/", ) // Get all data  
router.get("/:productId", ) //Get info for one product
router.post("/preview", ) //preview data
router.post("/", ) // publish data

export default router;
